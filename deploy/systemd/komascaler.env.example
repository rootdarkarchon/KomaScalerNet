# Environment-variable override example. Prefer the systemd credential for the token.
Upscaling__Models__CudaDeviceId=0
# TensorRT is mandatory. The fixed production policy is one active model,
# one session, one Run, core/context 832/64, and profile 8/960/960.
# Upscaling__Security__Token=replace-with-a-long-random-value
